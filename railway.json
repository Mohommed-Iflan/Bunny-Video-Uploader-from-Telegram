{
  "build": {
    "builder": "NIXPACKS",
    "config": {
      "startCommand": "python src/app.py"
    }
  },
  "deploy": {
    "startTimeout": 600000,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}